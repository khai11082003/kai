
import "./styles.scss";

const CannotTokenText = (props: any) => {
  return(
  <div className="cannot--token--text">
    Cannot find your token? <span >Import Token</span>
  </div>
  )
};

export default CannotTokenText;
